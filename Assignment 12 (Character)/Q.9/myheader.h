/*
	9. Accept Character from user and check whether it is special
	symbol or not (!, @, #, $, %, ^, &, *).
	Input : %
	Output : TRUE
	Input : d
	Output : FALSE
*/
#define TRUE 1
#define FALSE 2
typedef int BOOL;
BOOL ChkSpecial(char);