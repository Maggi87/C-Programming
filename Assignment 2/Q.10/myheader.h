/*
	10. Accept on character from user and check whether that character is vowel (a,e,i,o,u) or not.
	
	START
		Accept one character from user as ch
		if ch is equal equal to 'a','e','i','o','u','A','E','I','O','U'
			then print ch is vowel
		else 
			print ch is not vowel
	STOP
*/

#include<stdio.h>
void CkhVowel(char iChar);