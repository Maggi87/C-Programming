/*
	2. Write a program which accept number from user and print even factors of that number.
	Algorithm
	START
		Accept number from user as number
		if iNo is less than equal to zero or 
			then to convert iNo in positive number
		Iterate till iCnt is less than equal to iNo
			if two divide our iCnt 
				then print iCnt
			increment value of iCnt by one
			contiguous
	STOP		
		
*/

#include<stdio.h>
void PrintEven(int iNo);