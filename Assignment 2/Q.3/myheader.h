/*
	3. Write a program which accept two numbers from user and display its common factors.
	Algorithm
	START
		Accept two number from user as no1 and no2
	
		Iterate till iCnt is less than equal to iNo
			if two divide our iCnt 
				then print iCnt
			increment value of iCnt by one
			contiguous
	STOP		
		
*/

#include<stdio.h>
void PrintEven(int iNo1,int iNo2);