/*
	8. Accept one character from user and convert case of that character.
	
	START
		Accept one character from user as ch
		If character in a index is in lower case 
			then subtract 32 to convert it in upper case, 
		else 
			add 32 to convert it in lower case
	STOP
*/

#include<stdio.h>
void DisplayConvert(char iValue);

