/*
	Accept array from user accept one number from  and return last occurrence number of that number(using forward loop) 
	input : brr[6] = {5, 4 , 6 , 5 ,8} Search No : 5
	output : 3
	
	Algorithm
		START 
			consider element from array as iNo
			iterate from i to iNo	
				if i is equal to  iNo
						to store i value into iPos
				increment counter by one
			continue	

			return iPos
		STOP
*/

#include<stdio.h>
int DisplayLast(int *,int,int);
