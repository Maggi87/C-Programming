/*
	Accept array from user as well as accept one number from user and return the frequency of that number in array
	input : brr = {21,9,11,11,7,11,51} iNo = 11

	output : 3

	Algorithm

	STAR
	    take variable as iFrequency and initialize value 0
		Iterate the array from 1st element to last element 
			consider element from array as iNo
				f i equal to iNo
						increment iFrequency by one
				increment i by one			
		continue

		return iFrequency
	STOP
*/

int AdditionArray(int *,int);
