/*
	2) Accept string from user and display in reverse format
	input : hello
	output : olleh

	Algorithm

	START
		Accept String from user
		Iterate arra till end \0
			increase counter by one
		continue
		decreament arr by one
		iterate counter is less than 0
			printf the string
			descreament counter by one
		continue
	
*/
void ReverseDisplay(char *);